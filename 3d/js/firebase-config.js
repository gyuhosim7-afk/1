/* ============================================================
   구글 로그인 설정
   ------------------------------------------------------------
   Firebase 콘솔에서 웹 앱을 만들면 나오는 값을 그대로 붙여 넣으세요.
   비워 두면 로그인 단추가 숨겨지고 게스트로만 플레이합니다
   (게임 자체는 지금과 똑같이 돌아갑니다).

   여기 적는 값은 모두 '공개용' 입니다. 브라우저가 어차피 받아 가는
   값이라 저장소에 올려도 됩니다. 계정을 지키는 것은 이 값이 아니라
   Firebase 콘솔의 승인된 도메인과 Firestore 보안 규칙입니다.

   준비 순서는 README 의 '구글 로그인 붙이기' 를 보세요.
   ============================================================ */
const FIREBASE_CONFIG = {
  apiKey: '',
  authDomain: '',            // 예: last-survivor.firebaseapp.com
  projectId: '',
  appId: ''
};
